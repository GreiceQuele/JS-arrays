const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

//const nomesEmMaiusculos = nomes.map((nome) => {
    //return nome.toUpperCase();} )

const nomesEmMaiusculos = nomes.map((nome) => nome.toUpperCase()) ;  //mais usado no mercado 

console.log(nomesEmMaiusculos);